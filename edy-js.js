var links = document.querySelector('div.links');
const showMenu = () => {
    links.style.display = 'flex';
}
ScrollReveal({
    origin: 'top',
    distance: '30px',
    duration: 700
  }).reveal(`
   `)